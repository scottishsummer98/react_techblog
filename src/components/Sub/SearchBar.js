import React from "react";

const SearchBar = () => {
  return (
    <div>
      <input
        className="SearchBar_input"
        type="text"
        placeholder="Search here"
      />
    </div>
  );
};

export default SearchBar;
